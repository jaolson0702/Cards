using Cards;
using Tools;
using _27CardTrick;

string input = string.Empty;
do
{
    Console.Clear();
    Process.Run();
    input = "Type ENTER to run the program again or 'q' to quit: ".ReadLine();
    Console.WriteLine();
} while (input != "q");